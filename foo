
    const Interval Interval::P4 = Interval(Interval::Quality::perfect, 4, 5);

    const Interval Interval::P5 = Interval(Interval::Quality::perfect, 5, 7);

    const Interval Interval::P8 = Interval(Interval::Quality::perfect, 8, 12);

    const Interval Interval::P11 = Interval(Interval::Quality::perfect, 11, 17);

    const Interval Interval::P12 = Interval(Interval::Quality::perfect, 12, 19);

    const Interval Interval::P15 = Interval(Interval::Quality::perfect, 15, 24);

    const Interval Interval::m2 = Interval(Interval::Quality::minor, 2, 1);

    const Interval Interval::m3 = Interval(Interval::Quality::minor, 3, 3);

    const Interval Interval::m6 = Interval(Interval::Quality::minor, 6, 8);

    const Interval Interval::m7 = Interval(Interval::Quality::minor, 7, 10);

    const Interval Interval::m9 = Interval(Interval::Quality::minor, 9, 13);

    const Interval Interval::m10 = Interval(Interval::Quality::minor, 10, 15);

    const Interval Interval::m13 = Interval(Interval::Quality::minor, 13, 20);

    const Interval Interval::m14 = Interval(Interval::Quality::minor, 14, 22);

    const Interval Interval::M2 = Interval(Interval::Quality::major, 2, 2);

    const Interval Interval::M3 = Interval(Interval::Quality::major, 3, 4);

    const Interval Interval::M6 = Interval(Interval::Quality::major, 6, 9);

    const Interval Interval::M7 = Interval(Interval::Quality::major, 7, 11);

    const Interval Interval::M9 = Interval(Interval::Quality::major, 9, 14);

    const Interval Interval::M10 = Interval(Interval::Quality::major, 10, 16);

    const Interval Interval::M13 = Interval(Interval::Quality::major, 13, 21);

    const Interval Interval::M14 = Interval(Interval::Quality::major, 14, 23);

    const Interval Interval::d1 =
        Interval(Interval::Quality::diminished, 1, -1);

    const Interval Interval::d2 = Interval(Interval::Quality::diminished, 2, 0);

    const Interval Interval::d3 = Interval(Interval::Quality::diminished, 3, 2);

    const Interval Interval::d4 = Interval(Interval::Quality::diminished, 4, 4);

    const Interval Interval::d5 = Interval(Interval::Quality::diminished, 5, 6);

    const Interval Interval::d6 = Interval(Interval::Quality::diminished, 6, 7);

    const Interval Interval::d7 = Interval(Interval::Quality::diminished, 7, 9);

    const Interval Interval::d8 =
        Interval(Interval::Quality::diminished, 8, 11);

    const Interval Interval::d9 =
        Interval(Interval::Quality::diminished, 9, 12);

    const Interval Interval::d10 =
        Interval(Interval::Quality::diminished, 10, 14);

    const Interval Interval::d11 =
        Interval(Interval::Quality::diminished, 11, 16);

    const Interval Interval::d12 =
        Interval(Interval::Quality::diminished, 12, 18);

    const Interval Interval::d13 =
        Interval(Interval::Quality::diminished, 13, 19);

    const Interval Interval::d14 =
        Interval(Interval::Quality::diminished, 14, 21);

    const Interval Interval::d15 =
        Interval(Interval::Quality::diminished, 15, 23);

    const Interval Interval::A1 = Interval(Interval::Quality::augmented, 1, 1);

    const Interval Interval::A2 = Interval(Interval::Quality::augmented, 2, 3);

    const Interval Interval::A3 = Interval(Interval::Quality::augmented, 3, 5);

    const Interval Interval::A4 = Interval(Interval::Quality::augmented, 4, 6);

    const Interval Interval::A5 = Interval(Interval::Quality::augmented, 5, 8);

    const Interval Interval::A6 = Interval(Interval::Quality::augmented, 6, 10);

    const Interval Interval::A7 = Interval(Interval::Quality::augmented, 7, 12);

    const Interval Interval::A8 = Interval(Interval::Quality::augmented, 8, 13);

    const Interval Interval::A9 = Interval(Interval::Quality::augmented, 9, 15);

    const Interval Interval::A10 =
        Interval(Interval::Quality::augmented, 10, 17);

    const Interval Interval::A11 =
        Interval(Interval::Quality::augmented, 11, 18);

    const Interval Interval::A12 =
        Interval(Interval::Quality::augmented, 12, 20);

    const Interval Interval::A13 =
        Interval(Interval::Quality::augmented, 13, 22);

    const Interval Interval::A14 =
        Interval(Interval::Quality::augmented, 14, 24);

    const Interval Interval::A15 =
        Interval(Interval::Quality::augmented, 15, 25);